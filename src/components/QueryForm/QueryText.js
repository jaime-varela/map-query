import React from 'react'

export default () => {
    return (
        <div className="queryIntroText">
            <p> Use this page to find points of interest which are near one another.  When you hit 'Query' 
                the locations which match your search will be shown in red (first location) and blue 
                (second location).
            </p>
        </div>
    )
}