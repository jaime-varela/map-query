import React from 'react'

export default () => {
    return (
        <div className="queryIntroText">
            <p> Use this page to find points of interest which are near one another.  When you hit 'Query' 
                the locations markers will be shown.  Hover over to get location name and nearest neighbors 
                when distance is being filtered.
            </p>
        </div>
    )
}